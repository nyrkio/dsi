mock==4.0.3
pynose==1.5.0
pylint==3.1.0
# configparser is used by pylint and updates to it have caused failures, so pin it to a known
# good version (see https://jira.mongodb.org/browse/BF-11962).
configparser==3.5.0
yapf==0.28.0
testfixtures==8.1.0
yamllint==1.35.0
PyYAML~=6.0
coverage==4.5.4
future==0.16.0
